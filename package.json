{
  "name": "subql-dex",
  "version": "1.0.0",
  "description": "Subquery project to filter dex events and extrinsics",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Bruno Vale",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^4.6.2",
    "@subql/types": "latest",
    "typescript": "^4.1.3",
    "@subql/cli": "latest"
  }
}